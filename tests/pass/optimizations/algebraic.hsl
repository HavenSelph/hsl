/// bc: Identifier("a" Local@0)
/// bc: IntegerLiteral(0)
/// bc: IntegerLiteral(1)
/// out:
/// 5
/// 5
/// 0
/// 5
/// 5
/// 1

let a = 5

// x + 0 -> x (simplified to just 'a')
echo a + 0

// x * 1 -> x (simplified to just 'a')
echo a * 1

// x * 0 -> 0 (simplified to just '0')
echo a * 0

// x - 0 -> x (simplified to just 'a')
echo a - 0

// x / 1 -> x (simplified to just 'a')
echo a / 1

// x ** 0 -> 1 (simplified to just '1')
echo a ** 0
